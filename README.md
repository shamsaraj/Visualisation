# Visualisation (archived)

Moved to
[Virtual-Screening/evaluation/](https://github.com/shamsaraj/Virtual-Screening/tree/main/evaluation),
alongside the pipeline that actually produces the `results.csv` these
scripts consume, plus a glue script for converting it to the
`actives.csv`/`decoys.csv` format they expect. This repository is kept
around for history but isn't updated further.
